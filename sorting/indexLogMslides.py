def findInLogM(v):
        max = len(v) - 1
        symbol-m = v[max]      
        def boundInLogM(i):  
                if (symbol-m == v[max-i]:
                        bound-in-log-m(i*2)
                else:
                        return i